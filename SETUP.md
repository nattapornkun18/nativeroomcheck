# วิธีเชื่อมหน้าเว็บกับ Google Sheet

ใช้เวลาประมาณ 10 นาที ทำครั้งเดียวจบ

---

## 1. สร้างชีต

**วิธีที่แนะนำ — อัปไฟล์ Excel ที่จัดรูปแบบไว้แล้ว**

1. เข้า [drive.google.com](https://drive.google.com) → ใหม่ → อัปโหลดไฟล์ → เลือก `silicone-log.xlsx`
2. คลิกขวาที่ไฟล์ → เปิดด้วย → Google ชีต → ไฟล์ → บันทึกเป็น Google ชีต
3. จะได้ 2 แท็บ: **สรุป** (ตัวเลขคำนวณสด) และ **บันทึกตรวจ** (ข้อมูล 24 ห้อง)

ไฟล์นี้มีสีตามเกรด ช่องที่เป็นข้อบกพร่องขึ้นแดง/เหลืองอัตโนมัติ ตรึงหัวตาราง กรองได้ และมี dropdown ให้เลือกตอนกรอกมือ

**หรือถ้าอยากเริ่มจากศูนย์** ใช้ `silicone-log.csv` แทน: สร้างชีตใหม่ → ไฟล์ → นำเข้า → แทนที่สเปรดชีต แล้วเปลี่ยนชื่อแท็บเป็น **บันทึกตรวจ**

> ไม่ว่าจะใช้วิธีไหน แท็บข้อมูลต้องชื่อ **บันทึกตรวจ** ให้ตรงกับที่สคริปต์กำหนด

---

## 2. ติดตั้งสคริปต์

1. ในชีต เลือกเมนู **ส่วนขยาย → Apps Script**
2. ลบโค้ดเดิมที่ขึ้นมาทั้งหมด แล้ววางเนื้อหาจากไฟล์ `Code.gs` ลงไป
3. กดบันทึก (ไอคอนแผ่นดิสก์)

## 3. Deploy เป็น Web App

1. กด **Deploy → New deployment**
2. ที่ไอคอนเฟือง เลือก **Web app**
3. ตั้งค่า
   - Description: `silicone log`
   - Execute as: **Me**
   - Who has access: **Anyone**
4. กด Deploy แล้วอนุญาตสิทธิ์ (จะมีหน้าเตือน ให้กด Advanced → Go to project)
5. คัดลอกลิงก์ที่ลงท้ายด้วย `/exec` เก็บไว้

> ข้อควรระวัง: ต้องเลือก **Anyone** ไม่ใช่ Anyone with Google account ไม่งั้นหน้าเว็บจะเรียกไม่ได้

### ถ้าแก้ Code.gs ทีหลัง ต้อง deploy ใหม่ทุกครั้ง

กดบันทึกในตัวแก้ไขไม่พอ ลิงก์ `/exec` จะยังเสิร์ฟโค้ดเวอร์ชันที่ pin ไว้ตอน deploy ครั้งล่าสุด

**Deploy → Manage deployments → ไอคอนดินสอ → Version: New version → Deploy**

ทำแบบนี้ลิงก์เดิมใช้ได้ต่อ ไม่ต้องไปแก้ `API_URL` ใหม่ ถ้าเผลอกด New deployment จะได้ลิงก์เส้นใหม่ ต้องเอาไปเปลี่ยนใน `index.html` ด้วย

---

## 2.5 ตั้งค่าชีต — ดรอปดาวน์และสีเกรด

หลังวาง `Code.gs` แล้ว **ปิดชีตแล้วเปิดใหม่** จะมีเมนู **ซิลิโคน** โผล่ข้าง ๆ เมนูช่วยเหลือ

เลือก **ซิลิโคน → ตั้งค่าชีต** ครั้งเดียว ระบบจะจัดการให้ทั้งหมด

- ใส่ดรอปดาวน์ให้ทุกคอลัมน์ที่มีตัวเลือกตายตัว กรอกมือในชีตแล้วเลือกได้เลย พิมพ์เองไม่ได้
- ระบายสีช่องเกรด เขียว/เหลือง/แดง/เทา
- ตรึงหัวตาราง
- แปลงคำเก่าในคอลัมน์ขอบเขตงานให้ตรงกับหน้าเว็บ (`ครบสามด้าน` → `ยิงครบทุกด้าน`, `ขอบบนอย่างเดียว` → `ยิงขอบบนแค่ขอบบน (01&12)`)

รันซ้ำได้ตลอด ไม่มีผลเสีย ถ้าแก้รายการตัวเลือกใน `CHOICES` ท้ายไฟล์ก็รันใหม่อีกรอบให้ดรอปดาวน์อัปเดต

---

## 4. ใส่ลิงก์ในหน้าเว็บ

เปิด `index.html` หาบรรทัดที่ 3 ของสคริปต์ท้ายไฟล์

```js
const API_URL = "";
```

เปลี่ยนเป็น

```js
const API_URL = "https://script.google.com/macros/s/AKfy....../exec";
```

บันทึกไฟล์ เปิดในเบราว์เซอร์ ถ้าแถบเตือนสีเหลืองหายไปและตารางล่างขึ้นข้อมูล 24 ห้อง แปลว่าเชื่อมสำเร็จ

> ในรีโปนี้ใส่ลิงก์ไว้ให้แล้ว ทำขั้นตอนนี้ซ้ำเฉพาะตอน Deploy ใหม่แล้วได้ลิงก์ `/exec` เส้นใหม่

---

## 5. อัปขึ้น GitHub Pages

1. สร้าง repository ใหม่ เช่น `silicone-check`
2. อัปโหลด `index.html` เข้า repo (ต้องชื่อ index.html เท่านั้น หน้าแรกจึงจะขึ้น)
3. Settings → Pages → Source เลือก **Deploy from a branch** → branch `main` → folder `/ (root)`
4. รอ 1-2 นาที จะได้ลิงก์ `https://ชื่อผู้ใช้.github.io/silicone-check/`

---

## 6. แจ้งเตือนเข้า LINE

LINE Notify ปิดบริการไปแล้วตั้งแต่ มี.ค. 2025 ต้องใช้ **Messaging API** แทน

1. เข้า [developers.line.biz](https://developers.line.biz) ล็อกอินด้วยบัญชี LINE
2. สร้าง Provider → สร้าง Channel แบบ **Messaging API**
3. แท็บ Messaging API → เลื่อนล่างสุด → **Channel access token (long-lived)** → Issue → คัดลอกเก็บไว้
4. เอา QR ในหน้าเดียวกันไปแอดบอทเป็นเพื่อน หรือเชิญเข้ากลุ่มที่อยากให้แจ้งเตือน
5. ใน Apps Script วาง token ลงในตัวแปรบนสุดของ `Code.gs` แล้วกดบันทึก
6. **Deploy → Manage deployments → ดินสอ → New version → Deploy**
7. กลับมาที่ชีต เลือก **ซิลิโคน → ทดสอบส่งแจ้งเตือน LINE**

### เมนู ซิลิโคน → ตรวจสภาพการแจ้งเตือน LINE

ใช้ตอนแจ้งเตือนไม่เข้าแล้วไม่รู้ว่าติดตรงไหน ตรวจให้โดยไม่ต้องส่งข้อความจริง

- token ใช้ได้หรือถูก revoke ไปแล้ว
- ชื่อบัญชี OA ที่ token นี้ผูกอยู่ — กันกรณีวาง token ผิดช่อง
- โควตาข้อความเดือนนี้ ใช้ไปเท่าไหร่จากเท่าไหร่
- จำนวนเพื่อนของ OA (บัญชีที่ยังไม่ผ่านการรับรองจะขึ้น HTTP 403 ตรงนี้ ถือว่าปกติ ให้เช็คเองว่าแอด OA แล้วหรือยัง)
- โหมดแจ้งเตือนกับปลายทางที่ตั้งไว้จริงในตอนนี้
- แท็บข้อมูลมีอยู่ไหม กี่แถว

ถ้าทุกบรรทัดผ่านหมดแต่กดบันทึกในหน้าเว็บแล้วยังเงียบ แปลว่าแก้ `Code.gs` แล้วยังไม่ได้ Deploy เวอร์ชันใหม่

```js
var LINE_TOKEN = '';        // Channel access token
var LINE_TO    = '';        // เว้นว่าง = broadcast หาเพื่อนของ OA ทุกคน
var NOTIFY_WHEN = 'all';    // all · update · defect · off
```

### ปลายทาง `LINE_TO`

| ค่า | ผลลัพธ์ |
|---|---|
| เว้นว่าง | broadcast — ส่งหาทุกคนที่เป็นเพื่อนกับ OA นี้ |
| `Uxxxx…` | ส่งเข้าแชทส่วนตัวคนเดียว (Your user ID อยู่ในแท็บ Basic settings) |
| `Cxxxx…` / `Rxxxx…` | ส่งเข้ากลุ่มหรือห้องแชท ต้องดัก ID จาก webhook เอา |

### โหมด `NOTIFY_WHEN`

| โหมด | แจ้งเมื่อ |
|---|---|
| `all` | ทุกครั้งที่กดบันทึก ตรวจครั้งแรกส่งผลเต็ม แก้ไขส่งเฉพาะที่เปลี่ยน |
| `update` | เฉพาะตอนแก้ไขห้องที่เคยบันทึกไว้แล้ว และมีอะไรเปลี่ยนจริง |
| `defect` | เฉพาะห้องที่ผลออกมาเป็นไม่ผ่านหรือเกือบผ่าน |
| `off` | ปิดแจ้งเตือน ระบบอื่นทำงานปกติ |

ทุกโหมด ถ้าบันทึกทับด้วยค่าเดิมเป๊ะ ๆ จะไม่ส่ง กันข้อความกวนตอนกดซ้ำ

ข้อความตอนแก้ไขจะบอกว่าเปลี่ยนอะไรบ้าง

```
🔁 ห้อง 305 — แก้ไขผลตรวจ
❌ ไม่ผ่าน  →  ✅ ผ่าน
• กินผนัง: ฝั่งซ้าย → ไม่พบ
ผู้ตรวจ: พล
```

> **อย่า commit token ขึ้น GitHub** repo นี้เป็น public ใครก็เปิดอ่านได้ ให้วาง token เฉพาะในหน้าต่าง Apps Script เท่านั้น ถ้าเผลอวางในไฟล์แล้ว push ไปแล้ว ให้เข้า LINE Developers กด **Revoke** แล้ว Issue token ใหม่ทันที
>
> อีกทางถ้าอยากเก็บให้พ้นตัวไฟล์จริง ๆ: ใน Apps Script เลือก Project Settings → Script Properties → เพิ่ม `LINE_TOKEN` กับ `LINE_TO` ที่นั่น แล้วปล่อยตัวแปรในไฟล์ว่างไว้ โค้ดจะไปอ่านให้เอง

**สรุปรายวันตอนเย็น** ถ้าอยากได้ด้วย ใน Apps Script เลือกไอคอนนาฬิกา (Triggers) → Add Trigger → เลือกฟังก์ชัน `dailySummary` → Time-driven → Day timer → เลือกช่วงเวลา

---

## ตั้งค่าจำนวนชั้นและกฎขอบเขตงาน

แก้บล็อกตั้งค่าท้ายไฟล์ `index.html`

```js
const FLOOR_FROM = 3;          // ชั้นแรก
const FLOOR_TO   = 20;         // ชั้นสุดท้าย
const ROOMS_PER_FLOOR = 12;    // ห้องต่อชั้น
const TOP_ONLY = ["01","12"];  // ท้ายเลขห้องที่ยิงเฉพาะขอบบน
```

ตอนนี้ตั้งไว้ที่ชั้น 3 ถึง 20 ชั้นละ 12 ห้อง รวม 216 ห้อง ถ้าเปิดชั้นเพิ่มในอนาคตแก้แค่ `FLOOR_TO`

---

## การใช้งานประจำวัน

- เลือกห้อง ระบบเติมขอบเขตงานตามแบบให้เอง (ห้องลงท้าย 01 และ 12 = ยิงขอบบนแค่ขอบบน)
- กรอก 6 หัวข้อ แล้วดูแถบเกรดที่คำนวณสดด้านล่างฟอร์ม พร้อมเหตุผลว่าตกเพราะอะไร
- กดบันทึกลงชีต ข้อมูลจะต่อท้ายเป็นแถวใหม่เสมอ ไม่ทับของเดิม
- ตารางสรุปด้านล่างจะแสดง **รายการล่าสุดของแต่ละห้อง** ถ้าตรวจซ้ำรอบสอง ตัวเลขสรุปจะอัปเดตตามอัตโนมัติ

## เกณฑ์ที่ฝังไว้ในระบบ

| เกรด | เงื่อนไข |
|---|---|
| ผ่าน | ไม่พบข้อบกพร่องเลย |
| เกือบผ่าน | มีแค่ฟองอากาศ หรือผนังถลอก — แก้เฉพาะจุดได้ ไม่ต้องรื้อเส้น |
| ไม่ผ่าน | สีไม่ใช่แมตช์ · ยิงผิดขอบเขต (ห้อง 01/12 ที่ยิงด้านข้างมาด้วย หรือห้องอื่นที่ยิงไม่ครบสามด้าน) · ระยะขอบมีด้านหนึ่งหนากว่าผิดปกติ · ตัดขอบไม่เรียบ · กินผนัง · ยังไม่ได้ยิง |
| ไม่ได้ตรวจ | เลือกเหตุผลในหัวข้อ "การเข้าห้อง" ระบบจะข้ามการตัดเกรด |

ตัวเลือกในแต่ละหัวข้อ

| หัวข้อ | ตัวเลือก |
|---|---|
| ขอบเขตที่ยิง | ห้อง 01/12 → ยิงขอบบนแค่ขอบบน (01&12) / ยิงด้านข้างมาด้วย / ยังไม่ได้ยิง · ห้องอื่น → ยิงครบทุกด้าน / ยิงไม่ครบสามด้าน / ยังไม่ได้ยิง |
| สีที่ใช้ | แมตช์ · เงา · ยังไม่ได้ยิง |
| ระยะขอบ | เท่ากันทุกด้าน · มีด้านใดด้านหนึ่งหนากว่าผิดปกติ |
| ตัดขอบ | เรียบร้อย · เก็บไม่เรียบฝั่งซ้าย · เก็บไม่เรียบฝั่งขวา · เก็บไม่เรียบรอบด้าน |
| ฟองอากาศ | ไม่พบ · มี 1 จุด · มีหลายจุด |
| กินผนัง (ฝั่งหน้าหิน) | ไม่พบ · ฝั่งซ้าย · ฝั่งขวา · สองฝั่ง |
| สภาพผนัง | ปกติ · ถลอก - ต้องแตะสี |

ถ้าจะแก้เกณฑ์ภายหลัง ให้แก้ที่ฟังก์ชัน `judge()` ในไฟล์ `index.html`
