# วิธีติดตั้ง — ระบบเช็คห้องรวมหมวด

หน้าเว็บเดียว ชีตเดียว Apps Script ตัวเดียว รับได้ทุกหมวด
ตอนนี้มี 6 หมวด — **ม่าน · ซิลิโคนขอบอ่าง · น้ำร้อน & แอร์ · แสงรอบประตู · คิ้วบัวใต้ซิงค์ · หลืบห้องน้ำ**
เพิ่มหมวดใหม่ทีหลังได้โดยไม่ต้องทำเว็บใหม่

ใช้เวลาติดตั้งประมาณ 15 นาที ทำครั้งเดียวจบ

---

## ภาพรวม

```
หน้าเว็บ (index.html)                    Google Sheet
┌──────────────────┐                    ┌──────────────────────┐
│ เลือกหมวด        │                    │ แท็บ Summary          │
│   ↓              │   Apps Script      │ แท็บ Curtain & Sheer  │
│ เลือกห้อง        │ ─── Code.gs ────→  │ แท็บ Silicone         │
│   ↓              │                    │ แท็บ HW and A/C       │
│                  │                    │ แท็บ Door · Molding   │
│                  │                    │ แท็บ Bathroom         │
│ เช็ก → กดบันทึก  │ ──────────────────→└──────────────────────┘
└──────────────────┘   แจ้งเตือนเข้า LINE — 1 ครั้งที่กด = 1 ข้อความ
```

- ทุกหมวดใช้ **ลิงก์ Web App เส้นเดียวกัน** — ตั้งค่าที่เดียว
- แต่ละหมวดเก็บใน **แท็บของตัวเอง** ในชีตเดียวกัน คอลัมน์ไม่ปนกัน
- ในหน้าเช็กห้องหนึ่ง สลับหมวดไปมาได้ ของที่กรอกค้างไว้ไม่หาย
  **กดบันทึกครั้งเดียว = อัปขึ้นชีตทุกหมวดที่แก้ไว้ = ไลน์เด้งครั้งเดียว**

---

## 1. เตรียมชีต

ใช้ชีตเดิมได้เลย ไม่ต้องสร้างใหม่ ไม่ต้องย้ายข้อมูล

**ชื่อแท็บเป็นอังกฤษทั้งหมด** หนึ่งหมวดหนึ่งแท็บ

| หมวด | แท็บ | ชื่อไทยเดิม (ถ้ามี) |
|---|---|---|
| — | `Summary` | สรุปรวม |
| 🪟 ม่าน | `Curtain & Sheer` | บันทึกม่าน |
| 🧴 ซิลิโคนขอบอ่าง | `Silicone` | บันทึกตรวจ |
| 🌡️ น้ำร้อน & แอร์ | `HW and A/C` | บันทึกน้ำร้อนแอร์ |
| 🚪 แสงรอบประตู | `Door` | บันทึกแสงรอบประตู |
| 🪵 คิ้วบัวใต้ซิงค์ | `Molding` | บันทึกคิ้วบัวใต้ซิงค์ |
| 🚿 หลืบห้องน้ำ | `Bathroom` | — (หมวดใหม่ ยังไม่เคยมีแท็บเดิม) |

- **แท็บชื่อไทยของเดิมไม่ต้องแตะ** สคริปต์เปลี่ยนชื่อให้เองตอนรันครั้งแรก ข้อมูลเดิมอยู่ครบทุกแถว
  (สูตรในชีตเดียวกันที่อ้างแท็บนั้น Google อัปเดตให้เองตอนเปลี่ยนชื่อ
  ยกเว้น `IMPORTRANGE` จากชีตอื่นที่พิมพ์ชื่อแท็บเป็นข้อความ อันนั้นต้องไปแก้เอง)
- พิมพ์ชื่อแท็บต่างกันนิดหน่อยก็ยังจับได้ (`Curtain&Sheer` `curtain and sheer` ถือว่าแท็บเดียวกัน)
- ถ้าเผอิญมีสองแท็บที่เข้าข่าย สคริปต์เก็บแท็บที่มีข้อมูลมากกว่าไว้
  อีกแท็บจะโดนต่อท้ายชื่อว่า `(สำรอง)` ไม่มีอะไรถูกลบ
- แท็บของหมวดที่ยังไม่เคยบันทึก สคริปต์สร้างให้เองตอนบันทึกครั้งแรก
- คอลัมน์ใหม่ที่เพิ่มมา (เช่น `การเข้าห้อง`) สคริปต์จะ**ต่อท้าย**ให้เอง
  ไม่แทรกกลาง ข้อมูลกับสูตรเดิมจึงไม่เลื่อน

> ถ้าเริ่มจากชีตเปล่าก็ได้ — สคริปต์สร้างแท็บกับหัวตารางให้ครบเองทุกหมวด

### ย้ายข้อมูลม่านของเดิม (ถ้าเคยเก็บไว้คนละชีต)

ของเดิมหน้าม่านเก็บอยู่ในชีตอีกใบ ถ้าอยากรวมมาไว้ที่เดียวกัน

1. บันทึกม่านสักห้องผ่านหน้าเว็บก่อน เพื่อให้สคริปต์สร้างแท็บ **Curtain & Sheer** พร้อมหัวตาราง
2. เปิดชีตเก่า คัดลอกข้อมูล แล้ววางต่อท้ายในแท็บใหม่ให้ตรงคอลัมน์
3. คำเก่าอย่าง `PASS` / `DEFECT` ไม่ต้องแก้มือ — เลือกเมนู
   **เช็คห้อง → แปลงคำเก่าให้ตรงกับหน้าเว็บ** แล้วต่อด้วย
   **แปลงวันที่เป็นแบบไทย วัน/เดือน/ปี**

ไม่ย้ายก็ได้ ระบบใหม่เริ่มนับจากศูนย์ได้ตามปกติ ชีตเก่ายังอยู่ครบไม่ถูกแตะ

---

## 2. ติดตั้งสคริปต์

1. ในชีต เลือกเมนู **ส่วนขยาย → Apps Script**
2. ลบโค้ดเดิมที่ขึ้นมาทั้งหมด แล้ววางเนื้อหาจากไฟล์ `Code.gs` ลงไป
3. กดบันทึก (ไอคอนแผ่นดิสก์)

## 3. Deploy เป็น Web App

1. กด **Deploy → New deployment**
2. ที่ไอคอนเฟือง เลือก **Web app**
3. ตั้งค่า
   - Description: `room check`
   - Execute as: **Me**
   - Who has access: **Anyone**
4. กด Deploy แล้วอนุญาตสิทธิ์ (มีหน้าเตือน ให้กด Advanced → Go to project)
5. คัดลอกลิงก์ที่ลงท้ายด้วย `/exec` เก็บไว้

> ต้องเลือก **Anyone** ไม่ใช่ Anyone with Google account ไม่งั้นหน้าเว็บจะเรียกไม่ได้

### แก้ Code.gs ทีหลัง ต้อง deploy ใหม่ทุกครั้ง

กดบันทึกในตัวแก้ไขไม่พอ ลิงก์ `/exec` จะยังเสิร์ฟโค้ดเวอร์ชันที่ pin ไว้ตอน deploy ครั้งล่าสุด

**Deploy → Manage deployments → ไอคอนดินสอ → Version: New version → Deploy**

ทำแบบนี้ลิงก์เดิมใช้ได้ต่อ ไม่ต้องแก้ `API_URL` ใหม่
ถ้าเผลอกด New deployment จะได้ลิงก์เส้นใหม่ ต้องเอาไปเปลี่ยนใน `index.html` ด้วย

---

## 4. ตั้งค่าชีต — ดรอปดาวน์และสีเกรด

หลังวาง `Code.gs` แล้ว **ปิดชีตแล้วเปิดใหม่** จะมีเมนู **เช็คห้อง** โผล่ข้าง ๆ เมนูช่วยเหลือ

เลือก **เช็คห้อง → ตั้งค่าชีตทุกหมวด** ครั้งเดียว ระบบจะจัดหน้าตาให้ทุกแท็บเหมือนกันหมด

- **หัวตารางเขียวเข้ม ตัวหนังสือขาว** ตรึงหัว ตรึง 3 คอลัมน์แรก และเปิดตัวกรองให้
- **ระบายสีช่องที่เป็นข้อบกพร่องอัตโนมัติ** — แดงคือไม่ผ่านแบบต้องรื้อแก้ เหลืองคือไม่ผ่านแบบแก้เฉพาะจุด
  รวมถึงคอลัมน์ตัวเลข (เวลาน้ำร้อน เวลาแอร์ จำนวนจุดที่มีแสง) ที่ใช้เกณฑ์ตัวเลขแทนคำ
- ระบายสีช่องผลตรวจ เขียว = ผ่าน · เหลืองกับแดง = ไม่ผ่าน (คำเดียวกัน ต่างกันที่ความหนักเบา) · เทา = ไม่ได้ตรวจ
- ใส่ดรอปดาวน์กับสีให้คอลัมน์ **สถานะแก้ไข** — เหลือง = รอแก้ไข · น้ำเงิน = อนุโลม / แก้ไขแล้ว
  (หมวดที่แยกรอบตามแก้เป็นรายส่วน ช่องของแต่ละส่วนได้ดรอปดาวน์กับสีชุดเดียวกัน)
- ใส่ดรอปดาวน์ให้ทุกคอลัมน์ที่มีตัวเลือกตายตัว กรอกมือในชีตแล้วเลือกได้ พิมพ์เองไม่ได้
- ตั้งความกว้างคอลัมน์ จัดกึ่งกลางช่องตัวเลข ทำเส้นตาราง และตั้งวันที่เป็น วัน/เดือน/ปี
- แปลงคำเก่าให้ตรงกับหน้าเว็บ (`ครบสามด้าน` → `ยิงครบทุกด้าน`, `PASS` → `ผ่าน`,
  `เกือบผ่าน` → `ไม่ผ่าน` ฯลฯ)

> **ผลตรวจเหลือสองคำ: ผ่าน กับ ไม่ผ่าน**
> คำว่า *เกือบผ่าน* ถูกยุบเข้าไปรวมกับ *ไม่ผ่าน* แล้ว ความหนักเบาบอกด้วยสีอย่างเดียว
> — เหลืองคือแก้เฉพาะจุดได้ แดงคือต้องรื้อแก้ ชีตที่เคยใช้งานมาก่อนจะยังมีคำว่า
> `เกือบผ่าน` ค้างอยู่ในช่องผลตรวจ หน้าเว็บอ่านได้ปกติ (นับเป็นเหลือง) แต่ถ้าอยากให้
> ในชีตเป็นคำเดียวกันหมด ให้กด **เช็คห้อง → ตั้งค่าชีตทุกหมวด** หนึ่งครั้ง
> ระบบจะแปลงคำให้พร้อมระบายสีใหม่ทั้งคอลัมน์

แท็บที่สคริปต์สร้างเองจะได้หน้าตานี้ตั้งแต่แรกโดยไม่ต้องกดเมนู — กดเมนูเมื่ออยากจัดใหม่ทั้งชีต
หรือหลังแก้รายการตัวเลือกใน `Code.gs`

รันซ้ำได้ตลอด ไม่มีผลเสีย ทุกครั้งที่รันจะจัดกฎสีใหม่ทั้งแท็บ (ถ้าเคยตั้งสีเองไว้ในแท็บที่สคริปต์ดูแล จะถูกแทนที่ด้วยชุดมาตรฐาน)

เมนูอื่นที่มีให้

| เมนู | ใช้ตอนไหน |
|---|---|
| อัปเดตหน้าสรุป | อยากรีเฟรชแท็บ **Summary** เดี๋ยวนั้น (ปกติอัปเดตเองทุกครั้งที่มีคนกดบันทึก) |
| ล้างข้อมูลทั้งห้อง | อยากเริ่มตรวจห้องนั้นใหม่ ลบให้ครบทุกหมวดในครั้งเดียว |
| ตัดเกรดใหม่จากข้อมูลเดิม | เปลี่ยนเกณฑ์แล้ว อยากให้แถวเก่าถือเกณฑ์ใหม่โดยไม่ต้องเดินไล่กรอกใหม่ — คิดจากค่าที่กรอกไว้แล้ว เขียนทับเฉพาะช่องผลตรวจ |
| แปลงคำเก่าให้ตรงกับหน้าเว็บ | ข้อมูลเก่าใช้คำคนละแบบกับตัวเลือกในเว็บ |
| แปลงวันที่เป็นแบบไทย วัน/เดือน/ปี | แถวเก่าที่เก็บวันที่เป็น `2026-08-04` |
| จัดรูปแบบทุกแถวให้เหมือนกัน | แถวที่สคริปต์เขียนไว้หน้าตาไม่เหมือนแถวอื่น |

---

## 4.5 หน้าสรุป

มีสองที่ ดูอันไหนก็ได้ ข้อมูลชุดเดียวกัน

**ในชีต — แท็บ `Summary`** สคริปต์สร้างและอัปเดตให้เอง**ทุกครั้งที่มีคนกดบันทึก**
ไม่ต้องไปกดอะไร (หรือกด **เช็คห้อง → อัปเดตหน้าสรุป** ถ้าอยากรีเฟรชเดี๋ยวนั้น) มี 3 บล็อก

1. **ภาพรวมรายหมวด** — ตรวจแล้วกี่ห้อง เหลือกี่ห้อง คิดเป็นกี่ % แยกผ่าน / ปิดจบ (อนุโลม/แก้ไขแล้ว) /
   ไม่ผ่าน (เหลือง) / ไม่ผ่าน (แดง) / ไม่ได้ตรวจ
2. **ความคืบหน้ารายชั้น** — ชั้น 3–20 ว่าแต่ละหมวดตรวจไปกี่ห้องจากที่ต้องตรวจในชั้นนั้น
   (หมวดที่ข้ามบางห้องอยู่แล้ว เช่น คิ้วบัวใต้ซิงค์ ตัวหารจะเป็น 10 ไม่ใช่ 12)
3. **ห้องที่ยังรอแก้ไข** — รวมทุกหมวดไว้ที่เดียว บอกห้อง หมวด ผล เหตุผล วันที่ และผู้ตรวจ
4. **ปิดจบแล้ว — อนุโลม / แก้ไขแล้ว** — ห้องที่เคยไม่ผ่านแล้วปิดจบไปแล้ว บอกด้วยว่าเดิมไม่ผ่านตรงไหน

> ถ้าเคยทำแท็บ **สรุป** ไว้เอง สคริปต์ไม่ไปยุ่งด้วย — มันดูแลแค่แท็บ **Summary**
> ทุกครั้งที่อัปเดต แท็บนี้จะถูกเขียนทับทั้งแท็บ — อย่าพิมพ์อะไรเพิ่มลงไปเอง เดี๋ยวหาย

**ในหน้าเว็บ — ปุ่ม “📊 ดูภาพรวมทั้งหมด”** อยู่หน้าแรกใต้การ์ดหมวด
บอกว่าห้องไหนตรวจครบทุกหมวดแล้ว ห้องไหนยังไม่แตะเลย และห้องไหนต้องกลับไปแก้
แตะห้องในรายการเพื่อกระโดดเข้าไปแก้ได้เลย

แต่ละหมวดยังมีแท็บ **สรุป** ของตัวเองเหมือนเดิม (แยกตามชั้น แยกตามข้อบกพร่อง)

---

## 4.6 ข้อมูลวิ่งสองทางยังไง

| ทิศทาง | เกิดตอนไหน |
|---|---|
| เว็บ → ชีต | ตอนกดบันทึกเท่านั้น และเป็นการ**เพิ่มแถวใหม่**เสมอ ไม่เคยแก้หรือลบแถวเดิม |
| ชีต → เว็บ | อัตโนมัติทุก 25 วินาที · ตอนสลับกลับมาที่แท็บเบราว์เซอร์ · และทุกครั้งหลังกดบันทึก |

แก้หรือลบในชีตได้เลย เว็บจะตามให้เองภายในไม่เกินครึ่งนาที ไม่ต้องกดอะไร
(ตอนเปิดฟอร์มเช็กห้องค้างไว้จะหยุดดึงชั่วคราว กันของที่กำลังกรอกโดนเขียนทับ)

**ห้องหนึ่งมีได้หลายแถว** เพราะบันทึกทับ = เพิ่มแถวใหม่ **แถวล่างสุดของห้องนั้นคือค่าที่ใช้จริง**
ทั้งในเว็บและในหน้าสรุป แถวเก่าเก็บไว้เป็นประวัติ

### ล้างข้อมูลทั้งห้อง

ถ้าลบมือในชีตต้องไล่ลบทีละแท็บ และถ้าลบไม่หมดทุกแถว แถวเก่าจะเด้งกลับมาเป็นค่าล่าสุดแทน
ใช้เมนู **เช็คห้อง → ล้างข้อมูลทั้งห้อง (ทุกหมวดในครั้งเดียว)** แทน

1. ใส่เลขห้อง ใส่หลายห้องได้ คั่นด้วยเว้นวรรคหรือจุลภาค เช่น `304 305, 411`
2. ระบบบอกก่อนว่าจะลบกี่แถว หมวดไหนบ้าง แล้วค่อยกดยืนยัน
3. ลบครบทุกแถวทุกหมวด แล้วอัปเดตแท็บ `Summary` ให้เอง — ห้องนั้นกลับไปเป็น "ยังไม่ตรวจ"

เผลอลบผิด กด **Ctrl+Z** ในชีตทันทีก็ยังกู้กลับได้

---

## 4.7 ลิงก์แบบให้คนอื่นดูอย่างเดียว

ต่อท้าย URL ของหน้าเว็บด้วย `?view`

```
https://nattapornkun18.github.io/nativeroomcheck/?view
```

หน้านี้ดูได้ครบทุกอย่าง — ภาพรวมทุกหมวด หน้าสรุป รายการห้องที่ต้องแก้
และเปิดดูรายละเอียดรายห้องได้ว่าตอบอะไรไว้ — แต่

- ไม่มีช่องผู้ตรวจ ไม่มีปุ่ม "ทั้งหมดปกติ" ไม่มีแถบบันทึก
- ตัวเลือกทุกอันกดไม่ได้ กลายเป็นแค่ป้ายบอกว่าห้องนั้นตอบอะไรไว้
- ไม่ต้องใส่รหัสก็เปิดดูได้ ต่อให้ตั้ง `TOKEN` ไว้แล้ว

### ให้ "แก้ไม่ได้" จริง ๆ ไม่ใช่แค่ซ่อนปุ่ม

ลำพัง `?view` เป็นแค่การซ่อน UI คนที่รู้ลิงก์ `/exec` ยังยิงข้อมูลเข้าชีตเองได้
ถ้าอยากกันจริงให้ตั้งรหัสฝั่งเขียน — ใน `Code.gs`

```js
var TOKEN = '123';      // รหัสสำหรับ "บันทึก" ตั้งกี่หลักก็ได้
var READ_OPEN = true;   // แต่ "ดู" ยังเปิดให้ทุกคน
```

แล้วใน `index.html` ตั้ง `PIN_LEN` ให้เท่าจำนวนหลักของรหัส

```js
const PIN_LEN = 3;
```

ผลที่ได้

| ใคร | เปิดลิงก์แบบไหน | ทำอะไรได้ |
|---|---|---|
| คนตรวจ | ลิงก์ปกติ + ใส่รหัสหน้าล็อก (ใส่ครั้งเดียว เครื่องจำให้) | ดูและบันทึก |
| คนที่ให้ดูเฉย ๆ | ลิงก์ `?view` | ดูอย่างเดียว ยิงข้อมูลเข้าชีตไม่ได้เลย |

ถ้าตั้ง `READ_OPEN = false` จะกลายเป็นต้องใส่รหัสทั้งดูและบันทึก ลิงก์ `?view` ก็จะเปิดไม่ขึ้น

---

## 5. ใส่ลิงก์ในหน้าเว็บ

เปิด `index.html` หาบล็อกตั้งค่าในสคริปต์ท้ายไฟล์

```js
const API_URL = "https://script.google.com/macros/s/AKfy....../exec";
const API_TOKEN = "";     // ต้องตรงกับ TOKEN ใน Code.gs — เว้นว่าง = ไม่ล็อก
```

หรือจะไม่แก้ไฟล์ก็ได้ — เปิดหน้าเว็บแล้วแตะปุ่ม **เชื่อมชีต** มุมขวาบน วางลิงก์ลงไป
เครื่องนั้นจะจำไว้ให้ (เก็บใน localStorage ของเครื่อง)

อีกทางถ้าต้องตั้งหลายเครื่องรัว ๆ ส่งลิงก์แบบนี้ให้เปิดครั้งเดียวพอ

```
https://ชื่อผู้ใช้.github.io/repo/?api=<ลิงก์ /exec>&token=<รหัส>
```

---

## 6. อัปขึ้น GitHub Pages

1. อัปโหลด `index.html` เข้า repo (ต้องชื่อ index.html เท่านั้น หน้าแรกจึงจะขึ้น)
2. Settings → Pages → Source เลือก **Deploy from a branch** → branch → folder `/ (root)`
3. รอ 1-2 นาที จะได้ลิงก์ `https://ชื่อผู้ใช้.github.io/ชื่อ-repo/`

---

## 7. แจ้งเตือนเข้า LINE

LINE Notify ปิดบริการไปแล้วตั้งแต่ มี.ค. 2025 ต้องใช้ **Messaging API** แทน

1. เข้า [developers.line.biz](https://developers.line.biz) ล็อกอินด้วยบัญชี LINE
2. สร้าง Provider → สร้าง Channel แบบ **Messaging API**
3. แท็บ Messaging API → เลื่อนล่างสุด → **Channel access token (long-lived)** → Issue → คัดลอกเก็บไว้
4. เอา QR ในหน้าเดียวกันไปแอดบอทเป็นเพื่อน หรือเชิญเข้ากลุ่มที่อยากให้แจ้งเตือน
5. ใน Apps Script วาง token ลงในตัวแปรบนสุดของ `Code.gs` แล้วกดบันทึก
6. **Deploy → Manage deployments → ดินสอ → New version → Deploy**
7. กลับมาที่ชีต เลือก **เช็คห้อง → ทดสอบส่งแจ้งเตือน LINE**

```js
var LINE_TOKEN  = '';            // Channel access token
var LINE_TO     = '';            // เว้นว่าง = broadcast หาเพื่อนของ OA ทุกคน
var APP_TAG     = '[เช็คห้อง]';   // หัวข้อความ ใช้แยกว่ามาจากระบบไหน
var NOTIFY_WHEN = 'all';         // all · change · defect · off
```

### กติกาการแจ้งเตือน

**หนึ่งครั้งที่กดบันทึก = หนึ่งข้อความ** ไม่ว่าจะกดทีเดียวแล้วอัปเดตกี่หมวดก็ตาม

| ทำอะไร | ได้กี่ข้อความ |
|---|---|
| กรอกทั้งม่านทั้งซิลิโคน แล้วกดบันทึกทีเดียว | 1 ข้อความ มีทั้งสองหัวข้ออยู่ในนั้น |
| กดบันทึกม่านก่อน แล้วค่อยกดบันทึกซิลิโคน | 2 ข้อความ |
| กลับมาแก้ห้องเดิมแล้วกดบันทึกอีก | อีก 1 ข้อความ บอกเฉพาะช่องที่เปลี่ยน |
| เน็ตหลุดตอนกด แล้วระบบส่งให้ทีหลัง | ยังเป็น 1 ข้อความต่อการกดหนึ่งครั้งเหมือนเดิม |

ทุกข้อความมี **หัวข้อบอกว่าอัปเดตเรื่องอะไร** (`🪟 ม่าน` / `🧴 ซิลิโคนขอบอ่าง`)
และปิดท้ายด้วย **ชื่อผู้ตรวจกับวันที่แบบไทย วัน/เดือน/ปี** เสมอ

ตรวจครั้งแรก อัปเดตทีเดียวสองหมวด

```
[เช็คห้อง]
🏷️ ห้อง 305 · อัปเดต 2 หมวด

🪟 ม่าน — ❌ ไม่ผ่าน
   • สวิตช์ม่านทึบ: กดแล้วไม่ทำงาน

🧴 ซิลิโคนขอบอ่าง — ⚠️ ไม่ผ่าน
   • ฟองอากาศ: มี 1 จุด
   หมายเหตุ: รอช่างกลับมาเก็บ

— พล · 04/08/2026
```

กลับมาแก้ทีหลัง — บอกเฉพาะที่เปลี่ยน พร้อมวันที่ตรวจครั้งก่อน

```
[เช็คห้อง]
🏷️ ห้อง 305 · อัปเดต 1 หมวด

🧴 ซิลิโคนขอบอ่าง — 🔁 แก้ไขผลตรวจ
   ⚠️ ไม่ผ่าน  →  ✅ ผ่าน
   • ฟองอากาศ: มี 1 จุด → ไม่พบ
   (ตรวจครั้งก่อน 04/08/2026)

— พล · 05/08/2026
```

รอบตามแก้ — กดอนุโลมให้จุดที่ยอมรับได้ ค่าที่ตรวจไว้ยังอยู่เหมือนเดิม

```
[เช็คห้อง]
🏷️ ห้อง 305 · อัปเดต 1 หมวด

🪟 ม่าน — 🔁 แก้ไขผลตรวจ
   ❌ ไม่ผ่าน  →  ☑️ ผ่าน
   • สถานะแก้ไข: รอแก้ไข → อนุโลม
   (ตรวจครั้งก่อน 05/08/2026)

— พล · 12/08/2026
```

### โหมด `NOTIFY_WHEN`

| โหมด | แจ้งเมื่อ |
|---|---|
| `all` | ทุกครั้งที่กดบันทึก ← ค่าเริ่มต้น ตรงกับที่ใช้งานอยู่ |
| `change` | เฉพาะรอบที่มีค่าเปลี่ยนจริง กดทับด้วยค่าเดิมเป๊ะ ๆ จะเงียบ |
| `defect` | เฉพาะรอบที่มีหมวดไหนออกมาไม่ผ่าน (ทั้งเหลืองและแดง) |
| `off` | ปิดแจ้งเตือน ระบบอื่นทำงานปกติ |

### ปลายทาง `LINE_TO`

| ค่า | ผลลัพธ์ |
|---|---|
| เว้นว่าง | broadcast — ส่งหาทุกคนที่เป็นเพื่อนกับ OA นี้ |
| `Uxxxx…` | ส่งเข้าแชทส่วนตัวคนเดียว (Your user ID อยู่ในแท็บ Basic settings) |
| `Cxxxx…` / `Rxxxx…` | ส่งเข้ากลุ่มหรือห้องแชท ต้องดัก ID จาก webhook เอา |

### เมนู เช็คห้อง → ตรวจสภาพการแจ้งเตือน LINE

ใช้ตอนแจ้งเตือนไม่เข้าแล้วไม่รู้ว่าติดตรงไหน ตรวจให้โดยไม่ต้องส่งข้อความจริง —
token ยังใช้ได้ไหม · ผูกกับ OA ไหน · โควตาเดือนนี้ · จำนวนเพื่อน · โหมดกับปลายทางที่ตั้งไว้ ·
แต่ละหมวดมีกี่แถวในชีต

ถ้าทุกบรรทัดผ่านหมดแต่กดบันทึกในหน้าเว็บแล้วยังเงียบ แปลว่าแก้ `Code.gs` แล้วยังไม่ได้ Deploy เวอร์ชันใหม่

> **อย่า commit token ขึ้น GitHub** repo นี้เป็น public ใครก็เปิดอ่านได้
> ให้วาง token เฉพาะในหน้าต่าง Apps Script เท่านั้น
> ถ้าเผลอ push ไปแล้ว ให้เข้า LINE Developers กด **Revoke** แล้ว Issue token ใหม่ทันที
>
> อีกทางถ้าอยากเก็บให้พ้นตัวไฟล์: Apps Script → Project Settings → Script Properties →
> เพิ่ม `LINE_TOKEN` กับ `LINE_TO` ที่นั่น แล้วปล่อยตัวแปรในไฟล์ว่างไว้ โค้ดจะไปอ่านให้เอง

**สรุปรายวันตอนเย็น** ถ้าอยากได้ด้วย ใน Apps Script เลือกไอคอนนาฬิกา (Triggers) →
Add Trigger → ฟังก์ชัน `dailySummary` → Time-driven → Day timer → เลือกช่วงเวลา
(สรุปให้ทุกหมวดในข้อความเดียว)

---

## 8. ล็อกด้วยรหัสผ่าน (ถ้าต้องการ)

ใน `Code.gs`

```js
var TOKEN = '';     // เว้นว่าง = ใครเปิดลิงก์ก็ใช้ได้
```

ใส่รหัสแล้ว deploy ใหม่ หน้าเว็บจะขึ้นหน้าล็อกให้กดรหัสก่อนถึงจะอ่าน/เขียนชีตได้
แล้วแก้ `PIN_LEN` ใน `index.html` ให้ยาวเท่ากับรหัสที่ตั้งไว้

---

## การใช้งานประจำวัน

1. เปิดหน้าเว็บ ใส่ **ชื่อผู้ตรวจ** กับ **วันที่ตรวจ** (ขึ้นวันนี้ตามเวลาไทยให้อยู่แล้ว)
2. เลือกหมวด → เลือกชั้น → เลือกห้อง
3. กด **ทั้งหมดปกติ** แล้วค่อยแก้เฉพาะจุดที่เสีย เร็วกว่าไล่กดทีละหัวข้อ
4. อยากตรวจอีกหมวดของห้องเดียวกัน แตะแท็บหมวดด้านบนได้เลย ของที่กรอกไว้ไม่หาย
5. กด **บันทึก** ครั้งเดียว — ขึ้นชีตทุกหมวดที่แก้ไว้ และไลน์เด้งครั้งเดียว
6. ห้องเข้าไม่ได้ เลือกเหตุผลในหัวข้อ **การเข้าห้อง** ระบบจะบันทึกเป็น "ไม่ได้ตรวจ" และข้ามการตัดเกรด

ตรวจซ้ำห้องเดิมได้ตลอด ระบบต่อท้ายเป็นแถวใหม่เสมอ ไม่ทับของเดิม
ส่วนหน้าสรุปกับตารางบันทึกจะยึด **รายการล่าสุดของแต่ละห้อง**

---

## เกณฑ์ตัดเกรด

เหมือนกันทุกหมวด คิดจากตัวเลือกที่กด

| เกรด | เงื่อนไข |
|---|---|
| ผ่าน | ทุกหัวข้อเป็นค่าปกติ |
| ปิดจบ (น้ำเงิน) | มีจุดไม่ผ่าน แต่ช่อง **สถานะแก้ไข** เป็น *อนุโลม* หรือ *แก้ไขแล้ว* — ในชีตลงว่า ผ่าน |
| ไม่ผ่าน (เหลือง) | มีแต่ข้อที่เป็น *warn* — แก้เฉพาะจุดได้ ไม่ต้องรื้อทั้งชุด |
| ไม่ผ่าน (แดง) | มีข้อที่เป็น *bad* อย่างน้อยหนึ่งข้อ |
| ไม่ได้ตรวจ | เลือกเหตุผลในหัวข้อการเข้าห้อง |

### รอบตามแก้ — อนุโลม / รอแก้ไข / แก้ไขแล้ว

ตรวจไปแล้วรอบหนึ่ง พอมาดูของจริงหรือดูรูปอีกที บางจุดผู้ตรวจอนุโลมให้ผ่านได้
ทุกหมวดจึงมีคอลัมน์ **สถานะแก้ไข** เหมือนกันหมด อยู่ถัดจากหัวข้อตรวจ ก่อนช่องการเข้าห้อง

ในหน้าเว็บ การ์ด **Follow-up · รอบตามแก้** จะโผล่ **เฉพาะตอนที่รอบนั้นมีจุดไม่ผ่าน**
พร้อมลิสต์ให้เห็นว่าไม่ผ่านตรงไหนบ้าง ไม่ต้องเลื่อนขึ้นไปดู

| สถานะ | ผลกับห้องนั้น | ช่องผลตรวจในชีต |
|---|---|---|
| รอแก้ไข | ยังค้าง นับเป็นห้องที่ต้องเดินไปตาม | ไม่ผ่าน |
| อนุโลม | ผู้ตรวจยอมให้ผ่าน — ห้องนี้ปิดจบ | ผ่าน |
| แก้ไขแล้ว | ช่างแก้แล้ว ตรวจซ้ำเรียบร้อย — ห้องนี้ปิดจบ | ผ่าน |

- **ไม่แตะเลย = รอแก้ไข** ระบบลงคำว่า `รอแก้ไข` ให้เอง จะได้ไม่มีช่องว่างให้เดา
- **ค่าที่กรอกไว้ไม่ถูกลบ** ห้องที่อนุโลมยังเก็บไว้ครบว่าเดิมเสียตรงไหน ตามย้อนได้เสมอ
- **กดในชีตเองก็ได้** พิมพ์ (เลือกจากดรอปดาวน์) ว่า `อนุโลม` ในคอลัมน์สถานะแก้ไข
  ห้องนั้นถือว่าปิดจบทันที แล้วสั่ง **เช็คห้อง → ตัดเกรดใหม่จากข้อมูลเดิม** ให้ช่องผลตรวจตามมาเป็น `ผ่าน`
- **แก้จริงจนหายเสียแล้ว** จะเปลี่ยนค่าในหัวข้อนั้นให้เป็นค่าปกติไปเลยก็ได้
  การ์ดรอบตามแก้จะหายไปเอง ห้องนั้นกลับไปเป็น *ผ่าน* เขียวธรรมดา
- หน้าสรุปทั้งในเว็บและในชีตแยกเป็นสองตาราง **ห้องที่ยังรอแก้ไข** กับ **ปิดจบแล้ว (อนุโลม/แก้ไขแล้ว)**

**หมวดหลืบห้องน้ำแยกรอบตามแก้เป็นสองส่วน** ร่องหลืบกับหลืบพัดลมเป็นคนละงานคนละช่าง
จึงมีปุ่มให้กดคนละชุด ในชีตเป็นสองคอลัมน์ **สถานะแก้ไข ร่องหลืบ** กับ **สถานะแก้ไข หลืบพัดลม**
อยู่ก่อนคอลัมน์รวม **สถานะแก้ไข**

- ปุ่มของส่วนไหนโผล่ เฉพาะตอนส่วนนั้นมีจุดไม่ผ่านจริง ส่วนที่ยังดีอยู่ก็ไม่มีปุ่มมาเกะกะ
- อนุโลมส่วนหนึ่งไปก่อนแล้วอีกส่วนยังค้างได้ — **ห้องนี้จะปิดจบก็ต่อเมื่อทุกส่วนที่เสียปิดครบ**
- คอลัมน์รวม **สถานะแก้ไข** หน้าเว็บเติมให้เองจากสองช่องนี้ ผลตรวจของห้องยังตัดที่คอลัมน์รวม
  เหมือนทุกหมวด รายงานหรือสูตรเดิมที่อ่านคอลัมน์นี้จึงใช้ได้เหมือนเดิม
- ห้องที่บันทึกไว้ก่อนแยกส่วน มีแต่คอลัมน์รวม เปิดขึ้นมาหน้าเว็บเติมค่าเดิมลงทั้งสองส่วนให้เอง
- แก้สองช่องนี้ในชีตเอง คอลัมน์รวมจะไม่ตามให้ ต้องแก้คอลัมน์รวมด้วย
  (เหมือนช่องติดตามงานของทีมม่าน)

### หมวดม่าน

| หัวข้อ | ปกติ | ไม่ผ่าน · เหลือง (warn) | ไม่ผ่าน · แดง (bad) |
|---|---|---|---|
| ปิดม่านโปร่ง / ทึบ | ปิดสุด (เหลือระยะนิดหน่อยถือว่าปิดสุด) | — | ปิดเหลือระยะห่างกันมากกว่า 10cm |
| เปิดม่านโปร่ง / ทึบ | เปิดสุด | — | เปิดไม่สุด |
| สวิตช์โปร่ง / ทึบ | ปกติ | — | กดแล้วไม่ทำงาน |
| มือดึงโปร่ง / ทึบ (กระตุกกึ่งออโต้) | ปกติ | — | กระตุกแล้วไม่วิ่ง |
| ดึงมือไฟดับโปร่ง / ทึบ | ดึงมือได้สุด 100% | ดึงมือได้ไม่สุด | ดึงมือไม่ได้เลย |
| หยอด WD40 *(ไม่บังคับ)* | หยอดแล้ว · รางลื่นอยู่แล้ว ไม่ต้องหยอด | ยังไม่หยอด | — |
| เสียงโปร่ง / ทึบ | ปกติ | ดัง | — |
| เดินลื่นทั้งระบบ | ลื่น ไม่สะดุด | — | สะดุด/ฝืด |
| ชายม่านทึบถึงพื้น | ครึ่งนิ้ว | เกินครึ่งนิ้ว ไม่ถึง 1 นิ้ว | 1 นิ้วขึ้นไป |
| ลอนม่านทึบ | ลอนสวยปกติ | ลอนสวยแต่ยับ | ไม่เป็นลอนหรือลอนไม่เท่ากัน |
| แสงลอดม่านทึบ | ไม่มีแสงลอด | มีแสงลอด | ยังไม่ได้ติดตั้งผ้าม่าน |
| สถานะติดตั้งม่าน *(เติมให้เอง)* | ติดตั้งครบแล้ว | — | ยังติดตั้งไม่เสร็จ |

#### ห้องที่ผ้าม่านยังไม่ขึ้น — บันทึกส่วนที่ตรวจได้ไปก่อน

ไม่มีผืนผ้าก็ส่องหาแสงลอดไม่ได้ ข้อ **ปิดม่านทึบสุด ปิดไฟในห้อง แล้วดูรอบผืนม่าน**
จึงมีปุ่มที่สามว่า **ยังไม่ได้ติดตั้งผ้าม่าน** อยู่ตรงจังหวะที่ผู้ตรวจยืนอยู่หน้าม่านพอดี

- กด **ไม่มีแสงลอด** หรือ **มีแสงลอด** → การ์ด **Install & Finish งานติดตั้งและความเรียบร้อย**
  โผล่ขึ้นมาให้กรอกตามปกติ
- กด **ยังไม่ได้ติดตั้งผ้าม่าน** → การ์ดนั้น**หายไปทั้งใบ** (ตีนตุ๊กแก · ตะขอ · หัวสไลด์ ·
  ลอนม่าน · ชายม่าน) เพราะยังไม่มีผืนผ้าให้ดู แล้ว**กดบันทึกได้เลย**
  ไม่ต้องรอให้ตรวจครบทั้งหมด หัวข้อที่หายไปลงชีตเป็น `-`
- ห้องนั้นขึ้นว่า **ไม่ผ่าน** และเข้าคิว **🔧 ทีมช่างม่าน** จนกว่าผ้าจะขึ้น
  แล้วกลับมากดว่ามีหรือไม่มีแสงลอด — หัวข้อที่เหลือโผล่กลับมาครบเหมือนเดิม
  ค่าที่เคยกรอกไว้ไม่หาย
- คอลัมน์ **สถานะติดตั้งม่าน** ในชีตยังอยู่ที่เดิม ไม่ได้ถามเป็นข้อแยกแล้ว
  หน้าเว็บเติมให้เองจากปุ่มที่กด (`ยังไม่ได้ติดตั้งผ้าม่าน` → `ยังติดตั้งไม่เสร็จ`
  · อีกสองปุ่ม → `ติดตั้งครบแล้ว`) รายงานเก่าที่อ่านคอลัมน์นี้จึงยังใช้ได้เหมือนเดิม
  และห้องเก่าที่เคยกด *ยังติดตั้งไม่เสร็จ* ไว้ เปิดขึ้นมาระบบกดปุ่มใหม่ค้างไว้ให้แล้ว

#### หยอด WD40 — งานทีมโรงแรมที่ตามมาทำทีหลัง

**หยอด WD40** ไม่ใช่ผลตรวจงานช่างม่าน แต่เป็นงานที่ทีมโรงแรมตามมาทำทีหลัง
ทำได้ตั้งแต่มีรางแล้ว ไม่เกี่ยวว่าผ้าม่านขึ้นหรือยัง เลยแยกออกมาเป็น**การ์ดของตัวเอง**
อยู่นอกกำแพงสถานะติดตั้ง และตั้งเป็น**ไม่บังคับ**

- **ยังไม่ถึงคิว** → เว้นว่างไว้ กดบันทึกหมวดม่านไปก่อนได้เลย ไม่ตัดเกรด
  (ในชีตเป็นช่องว่าง สคริปต์ข้ามไม่คิดเป็นข้อบกพร่อง)
- **อยากให้ห้องนี้ค้างในคิวทีมโรงแรม** → กด `ยังไม่หยอด` ห้องนั้นขึ้น *ไม่ผ่าน* เหลือง
  และเข้ากอง 🏨 ทีมโรงแรม
- **ทำแล้ว** → กด `หยอดแล้ว` หรือ `รางลื่นอยู่แล้ว ไม่ต้องหยอด` ทั้งสองค่าถือว่าผ่าน
- ห้องที่มีสองราง (06/07) ต้องหยอดครบทั้งคู่ถึงจะนับว่าหยอดแล้ว

#### สองทีมที่เข้ามาแก้ — ระบบจับให้เอง

หมวดม่านมีคนสองกลุ่มที่เข้ามาแก้คนละเรื่อง หน้าเว็บดูจากข้อที่ไม่ผ่านแล้วจับทีมให้เอง
ผู้ตรวจไม่ต้องเลือกว่าใครทำ แค่กดว่าทีมนั้นแก้แล้วหรือยัง

| 🏨 ทีมโรงแรมแก้เอง | 🔧 ทีมช่างม่านมาแก้ |
|---|---|
| ชายม่าน **เกินครึ่งนิ้ว ไม่ถึง 1 นิ้ว** → ปรับตะขอเฉพาะจุด | ชายม่าน **1 นิ้วขึ้นไป** → ต้องแก้ความยาวผ้า |
| แสงลอด **ไม่เกิน 2 จุด และไม่ใช่รอยต่อกลาง** | แสงลอดที่ **รอยต่อกลาง** หรือ **ตั้งแต่ 3 จุด** |
| **ดึงมือตอนไฟดับ** ไม่ได้ 100% | **ลอนม่าน** ไม่เท่ากัน / ไม่เป็นลอน |
| **ยังไม่หยอด WD40** ที่ราง | **มอเตอร์ · สวิตช์ · กระตุกกึ่งออโต้** |
| | เปิด–ปิดไม่สุด · เสียงดัง · เดินสะดุด |
| | ตีนตุ๊กแก · ตะขอ · หัวสไลด์ |
| | **ยังไม่ได้ติดตั้งผ้าม่าน** |

ในชีตมีสองคอลัมน์ไว้ติดตามงาน อยู่ก่อนคอลัมน์ **สถานะแก้ไข**

| คอลัมน์ | ตัวเลือก |
|---|---|
| งานทีมโรงแรม | รอโรงแรมแก้ · โรงแรมแก้แล้ว |
| งานทีมช่างม่าน | รอช่างม่าน · ช่างม่านแก้แล้ว · อนุโลม ไม่ต้องแก้ |

ช่องพวกนี้**ไม่ตัดเกรด** เป็นแค่สถานะติดตามงาน — ผลตรวจของห้องยังตัดที่ **สถานะแก้ไข**
เหมือนหมวดอื่นทุกอย่าง ห้องจะปิดจบก็ต่อเมื่อกด *อนุโลม* หรือ *แก้ไขแล้ว* ที่ช่องนั้น

หน้าสรุปของหมวดม่านมีการ์ด **งานค้างแยกตามทีม** เพิ่มมา บอกว่าทีมไหนเหลือกี่ห้อง
ห้องไหนบ้าง เลขห้องสีเหลืองคือยังรอ สีน้ำเงินคือแก้แล้ว กดที่เลขห้องเพื่อเปิดห้องนั้นได้เลย

**แบรนด์เหลือ Smart curtain อย่างเดียวทั้งตึกแล้ว** หน้าเว็บจึงไม่มีให้เลือก ระบบเติมลงชีตให้เอง
ห้องเก่าที่เคยบันทึกว่า `Dooya` ค่าเดิมยังอยู่ในชีตเหมือนเดิม และไม่มีผลกับเกรดทั้งคู่

> **ตัวเลือกปิดม่านเปลี่ยนเกณฑ์** จากเดิมสามตัวเลือกเหลือสอง — *ปิดเหลือระยะนิดหน่อย*
> ถือว่าปิดสุด (ผ่าน) ส่วน *ปิดไม่ได้ เหลือครึ่งทาง* กลายเป็น *ปิดเหลือระยะห่างกันมากกว่า 10cm*
> หน้าเว็บอ่านค่าเก่าเป็นค่าใหม่ให้อยู่แล้ว ส่วนในชีตให้กด
> **เช็คห้อง → ตั้งค่าชีตทุกหมวด** (แปลงคำ + จัดดรอปดาวน์ใหม่) แล้วตามด้วย
> **ตัดเกรดใหม่จากข้อมูลเดิม** ครั้งเดียว แถวเก่าจะถือเกณฑ์ใหม่ทั้งหมด

> **คำที่เปลี่ยนรอบนี้** — แปลงให้เองด้วยเมนูเดียวกัน ไม่ต้องเดินกรอกห้องเก่าใหม่
> | คำเดิมในชีต | คำใหม่ |
> |---|---|
> | 1 นิ้ว | เกินครึ่งนิ้ว ไม่ถึง 1 นิ้ว |
> | มากกว่า 1 นิ้ว | 1 นิ้วขึ้นไป |
> | ดึงแล้วไม่ทำงาน | กระตุกแล้วไม่วิ่ง |
>
> ชายม่านแยกละเอียดขึ้นเพราะช่วงกลางกับช่วงเกิน 1 นิ้วคนละทีมกันมาแก้
> ส่วน *มือดึง* เดิมหมายถึงกระตุกแล้วมอเตอร์วิ่งให้ ไม่ใช่ดึงมือล้วน ๆ จึงเปลี่ยนคำให้ตรง
> แล้วแยก **ดึงมือไฟดับ** ออกมาเป็นหัวข้อใหม่ต่างหาก
>
> คอลัมน์ใหม่ 5 ช่อง (ดึงมือไฟดับโปร่ง/ทึบ · หยอด WD40 · งานทีมโรงแรม · งานทีมช่างม่าน)
> เมนู **ตั้งค่าชีตทุกหมวด** จะเติมหัวคอลัมน์ให้เอง ของเดิมในชีตไม่ถูกแตะ

### หมวดซิลิโคนขอบอ่าง

| หัวข้อ | ปกติ | ไม่ผ่าน · เหลือง (warn) | ไม่ผ่าน · แดง (bad) |
|---|---|---|---|
| ขอบเขตงาน | ห้อง 01/12 → ยิงขอบบนแค่ขอบบน · ห้องอื่น → ยิงครบทุกด้าน | — | ยิงผิดแบบ · ยังไม่ได้ยิง |
| สีที่ใช้ | แมตช์ | — | เงา · ยังไม่ได้ยิง |
| ระยะขอบ | เท่ากันทุกด้าน | — | มีด้านใดด้านหนึ่งหนากว่าผิดปกติ |
| ตัดขอบ | เรียบร้อย | — | ห้อง 01/12 → เก็บไม่เรียบ (ท่อนซ้าย/ท่อนกลาง/ท่อนขวา/ตลอดแนว) · ห้องอื่น → เก็บไม่เรียบ (ซ้าย/ขวา/รอบด้าน) |
| ฟองอากาศ | ไม่พบ | มี 1 จุด · มีหลายจุด | — |
| กินผนัง | ไม่พบ | — | ฝั่งซ้าย · ฝั่งขวา · สองฝั่ง |
| สภาพผนัง | ปกติ | ถลอก - ต้องแตะสี | — |

**ห้องที่ยังไม่ได้ยิง** กด "ยังไม่ได้ยิง" (ที่ขอบเขตงาน หรือที่สีที่ใช้ก็ได้) แล้วบันทึกได้เลย
หัวข้อคุณภาพที่เหลือจะถูกซ่อน ไม่ต้องกรอก และลงชีตเป็น `-` · เกรดของห้องนั้นคือ **ไม่ผ่าน**
พอกลับมายิงจริงแล้วเปลี่ยนเป็นยิงครบทุกด้าน หัวข้อคุณภาพจะกลับมาให้กรอกตามเดิม

---

### หมวดน้ำร้อน & แอร์

วัดตอนเข้าห้องครั้งแรกของวัน (ท่อยังเย็นอยู่) จะได้ค่าที่ตรงกับที่แขกเจอจริง

| หัวข้อ | ผ่าน | ไม่ผ่าน (เหลือง) | ไม่ผ่าน (แดง) |
|---|---|---|---|
| เปิดน้ำร้อนกี่วินาทีถึงร้อนพอ (เป้าหมาย 40–42 °C) | ≤ 30 วิ | 31–60 วิ | > 60 วิ |
| กี่นาทีห้องถึงเย็นถึงอุณหภูมิที่ตั้ง | ≤ 15 นาที | 16–30 นาที | > 30 นาที |
| อุณหภูมิที่ตั้งแอร์ | 23–25 °C | 22 หรือ 26 °C | นอกช่วงนั้น |
| อุณหภูมิน้ำที่วัดได้ (ไม่บังคับ) | 40–45 °C | 38–48 °C | นอกช่วงนั้น |
| อุณหภูมิห้องที่วัดได้ (ไม่บังคับ) | บันทึกไว้เฉย ๆ ไม่ตัดเกรด | | |

> น้ำร้อน 40–42 °C คืออุณหภูมิอาบสบายมาตรฐานโรงแรม ถังเก็บควรอยู่ที่ 60 °C ขึ้นไปเพื่อกันเชื้อลีจิโอเนลลา
> แต่ที่ปลายทางต้องคุมไม่ให้เกินราว 43 °C กันลวก · แอร์ 24 °C เป็นค่าที่ห้องพักส่วนใหญ่ตั้งไว้

แก้เกณฑ์ได้ที่บล็อกตั้งค่าใน `index.html`

```js
const HOT_TARGET = "40–42 °C";
const HOT_OK = 30, HOT_WARN = 60;   // วินาที
const AC_TARGET = 24;               // °C
const AC_OK = 15, AC_WARN = 30;     // นาที
```

ถ้าแก้ตัวเลขพวกนี้ ให้ไปแก้ `num` ของหมวด `comfort` ใน `Code.gs` ให้ตรงกันด้วย
(ใช้ระบายสีในชีต ไม่ได้ใช้ตัดเกรด)

### หมวดแสงรอบประตู

ปิดไฟในห้อง เปิดไฟทางเดิน แล้วดูว่ามีแสงลอดตรงไหน

1. เลือก **ไม่มีแสงเล็ดลอด** → ผ่าน จบ
2. เลือก **มีแสงเล็ดลอด** → ผังประตูจะโผล่มา แตะช่องที่เห็นแสง แตะซ้ำเพื่อเอาออก

| จำนวนจุดที่มีแสง | ผล |
|---|---|
| 0 | ผ่าน |
| 1–2 จุด | ไม่ผ่าน (เหลือง) |
| 3 จุดขึ้นไป | ไม่ผ่าน (แดง) |

**ลงชีตเป็นข้อความล้วน ไม่เก็บรูป** — ช่องถูกตั้งชื่อจากซ้ายไปขวาและบนลงล่าง
(`บน1–บน4`, `ล่าง1–ล่าง5`, `ซ้าย1–ซ้าย8`, `ขวา1–ขวา8`) แล้วเก็บเป็น

```
ล่าง1 มีแสง · ล่าง2 มีแสง · ซ้าย4 มีแสง
```

พร้อมคอลัมน์ `จำนวนจุดที่มีแสง` ไว้กรอง/เรียงในชีต และ `บานประตู` บอกว่าห้องนั้นมือจับซ้ายหรือขวา

แก้จำนวนช่องกับกลุ่มห้องได้ที่ `index.html`

```js
const DOOR = {top:4, bottom:5, left:8, right:8};      // จำนวนช่องแต่ละด้าน
const DOOR_LEFT = ["01","02","03","05","06","09"];    // ห้องที่มือจับอยู่ซ้าย ที่เหลืออยู่ขวา
```

### หมวดคิ้วบัวใต้ซิงค์

คิ้วบัวใต้ซิงค์อ่างล้างหน้า ดูสามช่วงตามที่เห็นเวลาก้มมองจากหน้าอ่าง
**บัวซ้าย · บัวกลาง · บัวขวา** (ซ้าย/ขวาคือช่วงที่เดินตามขาไม้ที่สอบออกสองข้าง)
พร้อมกับ **ช่องรูระหว่างผนังกับพื้น** อีกสามจุดในแนวเดียวกัน

หน้าเว็บวาดผังใต้ซิงค์ให้ดูควบคู่ไปด้วย วางตำแหน่งตามที่เห็นหน้างานจริง

| ในผัง | คืออะไร |
|---|---|
| แท่งน้ำตาลตั้งสองอัน | ขาไม้ |
| เส้นหนาที่เดินซ้าย–กลาง–ขวา | คิ้วบัว เขียว = เรียบร้อย · แดง = ขอบไม่สุดขาไม้ · แดงประ = ยังไม่ได้ติด · เทาประ = ห้อง `02`/`11` ที่ไม่มีบัวตามแบบ |
| เส้นบางด้านในที่ขนานไปกับบัว | แนวผนังชนพื้น ช่วงไหนมีช่องรูจะเน้นเป็นเส้นหนาสีส้ม |

ช่วงที่ตอบว่า **ขอบบัวไม่สุดขาไม้** ผังจะเว้นช่วงตรงโคนขาไม้ไว้
แล้วคาเส้นประไว้ตรงนั้นว่าคือส่วนที่บัวเดินไปไม่ถึง (บัวกลางมีขาไม้ทั้งสองปลาย เว้นให้ทั้งสองข้าง)

**เกณฑ์รับงาน** ผ่านเมื่อ **ติดบัวครบทุกช่วง ขอบเดินไปสุดขาไม้ และไม่มีช่องรูสักจุด**

| หัวข้อ | ปกติ | ไม่ผ่าน · เหลือง (warn) | ไม่ผ่าน · แดง (bad) |
|---|---|---|---|
| บัวซ้าย / กลาง / ขวา | ติดเรียบร้อย | — | ขอบบัวไม่สุดขาไม้ · ไม่ได้ติดบัว |
| ช่องรูซ้าย / กลาง / ขวา | ไม่มีช่องรู | มีช่องรู | — |

**ห้องไหนตรวจอะไรบ้าง**

| ห้อง | ตรวจ |
|---|---|
| `01` `12` (ห้องมุม) | ไม่ต้องมีเรคคอร์ดเลย — ในหน้าเลือกห้องจะขึ้นเป็นช่องเทา “ไม่มี” และปุ่ม ‹ › จะข้ามให้ |
| `02` `11` | ไม่มีบัวตามแบบ — หน้าเว็บซ่อนหัวข้อบัวให้ เหลือแค่ช่องรูสามจุด (ช่องบัวในชีตลงเป็น `-`) |
| ห้องอื่น | ครบทั้งหกหัวข้อ |

**ไม่ได้ติดบัว = ข้อบกพร่อง** เพราะเกณฑ์รับงานคือต้องติดครบทุกด้าน
ห้องที่ยังติดไม่ครบจะขึ้นเป็นห้องที่ต้องกลับไปแก้ และในผังจะเป็นเส้นประสีแดง
ข้อยกเว้นมีทางเดียวคือห้อง `02` กับ `11` ที่ตามแบบไม่มีบัว — หน้าเว็บซ่อนหัวข้อบัวให้อยู่แล้ว
ช่องบัวในชีตลงเป็น `-` จึงไม่ถูกนับเป็นข้อบกพร่อง

ตัวเลือกอยู่ที่ `index.html` ที่เดียว ค่าปกติคือตัวแรก

```js
const MOLD_OPTS = [{v:"ติดเรียบร้อย"},{v:"ขอบบัวไม่สุดขาไม้",bad:1},{v:"ไม่ได้ติดบัว",bad:1}];
```

แก้กลุ่มห้องได้ที่ `index.html`

```js
const MOLD_NONE = ["01","12"];   // ไม่ต้องมีเรคคอร์ดหมวดนี้เลย
const MOLD_GAP  = ["02","11"];   // ไม่มีบัวตามแบบ ตรวจแค่ช่องรู
```

ถ้าแก้ `MOLD_NONE` ให้ไปแก้ `skip` ของหมวด `molding` ใน `Code.gs` ให้ตรงกันด้วย
(ใช้คิด % กับตัวหารในหน้าสรุปของชีต)

### หมวดหลืบห้องน้ำ

**เกณฑ์รับงาน** ผ่านเมื่อครบทั้งสามข้อ

- ปูกระเบื้องไปสุดขอบร่อง shadow gap
- ในร่อง shadow gap **ไม่ทาสี**
- หลืบ exhaust air ทาสีจบที่ในหลืบ **ไม่ทาลงมาบนกระเบื้องผนัง**

ไม่มีรูปประกอบในฟอร์ม เพราะหน้างานแต่ละห้องไม่เหมือนกัน ตอบตามที่เห็นจริงในห้องนั้น

**1. Shadow gap — ร่องหลืบขอบผนังห้องน้ำ** สองคำถามที่แยกกัน ต้องผ่านทั้งคู่

| หัวข้อ | ปกติ | ไม่ผ่าน · แดง (bad) |
|---|---|---|
| แนวกระเบื้องเดินถึงขอบร่องไหม | กระเบื้องสุดขอบ | กระเบื้องไม่สุดขอบ |
| ในร่องทาสีไหม สีอะไร | ไม่ทาสี | ทาสีน้ำตาล · ทาสีขาว |

หน้าเว็บรวมสองช่องนี้ให้เองเป็นคอลัมน์ `แบบ shadow gap` ในชีต
(เช่น `กระเบื้องไม่สุดขอบ + ทาสีน้ำตาล`) จะได้ pivot นับได้เลยว่าแบบไหนมีกี่ห้อง
คอลัมน์นี้เป็นคอลัมน์สรุปเฉย ๆ ไม่ได้ถูกตัดเกรดซ้ำ

**2. Exhaust air — ช่องหลืบพัดลมดูดอากาศ**

| หัวข้อ | ปกติ | ไม่ผ่าน · แดง (bad) |
|---|---|---|
| สีในหลืบทาลงมาถึงผนังไหม | ทาสีไม่ลงมาผนัง | ทาสีลงมาผนัง |

ตัวเลือกอยู่ที่ `index.html` ที่เดียว ค่าปกติคือตัวแรกของแต่ละแถว

```js
const SG_TILE_OPTS  = [{v:"กระเบื้องสุดขอบ"},{v:"กระเบื้องไม่สุดขอบ",bad:1}];
const SG_PAINT_OPTS = [{v:"ไม่ทาสี"},{v:"ทาสีน้ำตาล",bad:1},{v:"ทาสีขาว",bad:1}];
const EX_PAINT_OPTS = [{v:"ทาสีไม่ลงมาผนัง"},{v:"ทาสีลงมาผนัง",bad:1}];
```

ถ้าแก้ตัวเลือกตรงนี้ ให้แก้ `choices` ของหมวด `bathgap` ใน `Code.gs` ให้เรียงตรงกันด้วย
(ตัวแรก = ค่าปกติ ใช้ทั้งดรอปดาวน์และสีในชีต)

**ข้อมูลที่เก็บไว้แล้วไม่ต้องเดินไล่กรอกใหม่** แต่ช่อง `ผลตรวจ` ของแถวเก่ายังค้างเป็นเกรดที่
ตัดไว้ตอนกดบันทึก (ตอนนั้นสองหมวดนี้ยังไม่ตัดเกรดตามเกณฑ์นี้) ให้เปิดชีตแล้วเลือก
**เช็คห้อง → ตัดเกรดใหม่จากข้อมูลเดิม** ครั้งเดียว ระบบจะคิดเกรดใหม่จากค่าที่กรอกไว้แล้วทุกแถว
(บอกก่อนว่าจะเปลี่ยนกี่แถว หมวดไหนบ้าง แล้วค่อยยืนยัน) ห้องที่ทำมาไม่ตรงเกณฑ์
จะขึ้นในรายการห้องที่ต้องกลับไปแก้ให้เองทั้งในเว็บและในแท็บ `Summary`

สีในชีตมาจาก `choices` เหมือนกัน ถ้าอยากให้ช่องเก่าเปลี่ยนสีตามเกณฑ์ใหม่ด้วย
ให้สั่ง **เช็คห้อง → ตั้งค่าชีต** อีกครั้ง (วางกฎสีกับดรอปดาวน์ใหม่ ไม่แตะข้อมูล)

---

## ตั้งค่าจำนวนชั้นและกฎห้อง

แก้บล็อกตั้งค่าในสคริปต์ท้ายไฟล์ `index.html`

```js
const FLOOR_FROM = 3;          // ชั้นแรก
const FLOOR_TO   = 20;         // ชั้นสุดท้าย
const ROOMS_PER_FLOOR = 12;    // ห้องต่อชั้น
const TOP_ONLY = ["01","12"];  // ท้ายเลขห้องที่ซิลิโคนยิงเฉพาะขอบบน
const ROOM_TYPE = { 1:"grand corner", ... };   // ประเภทห้องตามท้ายเลขห้อง
```

ตอนนี้ตั้งไว้ชั้น 3 ถึง 20 ชั้นละ 12 ห้อง รวม 216 ห้อง เปิดชั้นเพิ่มก็แก้แค่ `FLOOR_TO`

### ผังชั้นในหน้าเลือกห้อง

หน้าเลือกห้องวางปุ่มตามตำแหน่งจริงในแปลน ไม่ใช่เรียงเป็นตาราง 1–12
จะได้กดห้องจากที่ยืนอยู่ได้เลยโดยไม่ต้องแปลงเลขห้องในหัว
เป็นผังย่อ มีแค่แนวผนังกั้นห้อง ไม่ได้ลงรายละเอียดในห้อง

```
06 ┃         ┃ 07     ← หัวอาคาร
05 ┃  ลิฟต์   ┃ 08
04 ┃  บันได   ┃ 09
03 ┃  ช่าง    ┃ 10
01 ┃ 02 ┃ 11 ┃ 12     ← ท้ายอาคาร 01 กับ 12 เป็นห้องมุม
```

แก้ตำแหน่งได้ที่ตาราง `PLAN` ใน `index.html` — คีย์คือท้ายเลขห้อง
ค่าคือ `[คอลัมน์, แถว]` ในกริด 4 คอลัมน์ × 5 แถว นับจากมุมซ้ายบน
คอลัมน์ 2–3 แถว 1–4 กันไว้ให้แกนกลางอาคาร (แก้ข้อความในแกนที่ `CORE_TEXT`)

```js
const PLAN = {
  6:[1,1],  7:[4,1],
  5:[1,2],  8:[4,2],
  ...
  1:[1,5],  2:[2,5], 11:[3,5], 12:[4,5]
};
```

ถ้าห้องไหนไม่มีในตาราง `PLAN` หน้าเว็บจะถอยไปใช้ตารางเรียงธรรมดาให้เองทั้งชั้น
(เผื่อวันหลังเปลี่ยน `ROOMS_PER_FLOOR` แล้วยังไม่ได้อัปเดตผัง)

---

## เพิ่มหมวดใหม่ในอนาคต

ทำสองที่ ชื่อคอลัมน์ต้องตรงกันเป๊ะ ไม่ต้องแตะโค้ดส่วนอื่นเลย

**1. `index.html`** เพิ่มก้อนใน `TOPICS` — ตัวเลือกตัวแรกของแต่ละหัวข้อคือค่าปกติ
`bad` คือไม่ผ่านสีแดง `warn` คือไม่ผ่านสีเหลือง

```js
{
  id:"aircon", name:"แอร์", en:"Aircon", icon:"❄️", grade:"ผลตรวจ",
  sections:[
    {cap:"Cooling · ความเย็น", fields:[
      {col:"ความเย็น", label:"ความเย็น",
       opts:[{v:"เย็นปกติ"},{v:"เย็นช้า",warn:1},{v:"ไม่เย็น",bad:1}]},
      {col:"น้ำหยด", label:"น้ำหยด",
       opts:[{v:"ไม่มี"},{v:"มีน้ำหยด",bad:1}]}
    ]}
  ]
}
```

**2. `Code.gs`** เพิ่มก้อนใน `TOPICS` ให้ชื่อคอลัมน์ตรงกัน

```js
aircon: {
  name:'แอร์', en:'Aircon', icon:'❄️',
  sheet:'บันทึกแอร์',
  grade:'ผลตรวจ',
  auto: [],
  cols:['ความเย็น','น้ำหยด', FIX, ACCESS, 'ผลตรวจ'],
  choices:{
    'ความเย็น':['เย็นปกติ','เย็นช้า','ไม่เย็น'],
    'น้ำหยด':['ไม่มี','มีน้ำหยด']
  }
}
```

`FIX` (คอลัมน์ **สถานะแก้ไข**) กับ `ACCESS` ใส่ต่อท้ายทุกหมวดเหมือนกันหมด
ฝั่ง `index.html` ไม่ต้องเขียนอะไรเพิ่ม — สองหัวข้อนี้ระบบต่อท้ายให้เองทุกหมวด

อยากให้หมวดใหม่**ข้ามบางห้อง**ไปเลย (ไม่ต้องมีเรคคอร์ด ไม่นับเป็นห้องที่ค้าง)
ใส่ `na` ใน `index.html` กับ `skip` ใน `Code.gs` ให้ตรงกัน

```js
// index.html
na: r => ["01","12"].indexOf(String(r).slice(-2)) !== -1,
// Code.gs
skip: ['01','12'],
```

อยากให้บางหัวข้อโผล่เฉพาะบางห้อง ใส่ `showIf` ในหัวข้อนั้น — ช่องที่ไม่โผล่จะลงชีตเป็น `-`

```js
showIf: (v, r) => String(r).slice(-2) !== "02",
```

แล้ว deploy เวอร์ชันใหม่ + เปิดชีตเลือก **เช็คห้อง → ตั้งค่าชีตทุกหมวด** อีกครั้ง
หมวดใหม่จะโผล่ในหน้าเลือกหมวด มีแท็บของตัวเองในชีต และเข้าไปอยู่ในข้อความแจ้งเตือนเหมือนหมวดอื่นทันที

---

## เวลาแจ้งเตือนไม่เข้า / ข้อมูลไม่ขึ้น

| อาการ | สาเหตุที่เจอบ่อย |
|---|---|
| กดบันทึกแล้วขึ้น "ส่งไม่สำเร็จ เก็บไว้ในเครื่องก่อน" | เน็ตหลุด — ระบบเก็บไว้ให้แล้ว พอเน็ตกลับมาจะส่งเอง ปุ่มมุมขวาบนจะขึ้น "ค้าง n" |
| ข้อมูลไม่ขึ้นชีตเลย | ตอน deploy เลือก Who has access ไม่ใช่ Anyone |
| แก้ Code.gs แล้วไม่มีอะไรเปลี่ยน | ยังไม่ได้ Deploy → Manage deployments → New version |
| ไลน์เงียบทั้งที่ทุกอย่างปกติ | เมนู เช็คห้อง → ตรวจสภาพการแจ้งเตือน LINE จะบอกว่าติดตรงไหน |
| หน้าเว็บขึ้นหน้าล็อก | `TOKEN` ใน Code.gs ไม่ตรงกับรหัสที่กด |
